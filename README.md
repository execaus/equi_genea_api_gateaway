# Equi Genea API Gateway

## Конфигурация

```yaml
server:
  allowOrigin: "http://localhost:5173"
  port: "8080"

services:
  account:
    host: "localhost"
    port: "9090"
  auth:
    host: "localhost"
    port: "9090"
```